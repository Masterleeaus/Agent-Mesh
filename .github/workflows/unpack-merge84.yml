name: Unpack Merge84

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/unpack-merge84.yml"

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Runner smoke test
        run: echo "runner-ok"
