import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";
describe("live Interface Runtime context contract",()=>{const src=readFileSync(join(process.cwd(),"lib/titan/interface-runtime/live-context.ts"),"utf8");it("uses authenticated sessions and company-scoped queries",()=>{expect(src).toContain("getSession");expect(src).toContain("account_id=$1");expect(src).toContain("company_id:accountId")});it("keeps Go technician jobs assignment-scoped",()=>expect(src).toContain("v.assigned_user_id=$2"));it("keeps Hub client scoped",()=>{expect(src).toContain("getPortalSession");expect(src).toContain("client_id=$2")});it("does not add business writes",()=>expect(src).not.toMatch(/INSERT INTO|UPDATE .* SET|DELETE FROM/))});
