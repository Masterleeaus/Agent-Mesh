# Flutter tool metadata.
#
# This repository intentionally does not fabricate Flutter framework revisions.
# Regenerate this file with the pinned Flutter SDK when the canonical mobile
# build pipeline/toolchain is established.

project_type: app

migration:
  platforms:
    - platform: root
    - platform: android
    - platform: ios
