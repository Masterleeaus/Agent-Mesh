const fs=require('fs'),vm=require('vm'),assert=require('assert');
const source=fs.readFileSync('src/lib/service-worker.js','utf8');const chrome={sidePanel:{setPanelBehavior:async()=>{}},runtime:{onMessage:{addListener(){}},onStartup:{addListener(){}},onInstalled:{addListener(){}},sendMessage:async()=>({})},alarms:{get:async()=>null,create:async()=>{},onAlarm:{addListener(){}}},tabs:{query(_q,cb){cb([])}},storage:{local:{get:async()=>({}),set:async()=>{}},sync:{get:async()=>({})}}};const c={chrome,console:{log(){},warn(){},error(){}},setTimeout,clearTimeout,Map,Set,Promise,Date,Math,crypto:{randomUUID:()=> 'x'}};vm.runInNewContext(source,c);const history=Array.from({length:500},(_,i)=>({artifactId:`a${i}`,sha256:String(i).padStart(64,'a').slice(-64)}));const p=c.normalizePlanState({stateVersion:2,protocolMode:'signature_v2',planId:'p',runId:'r',plan:[{number:1,text:'x'}],stepIndex:0,artifactHistory:history,consumedArtifactHashes:Array.from({length:500},(_,i)=>i.toString(16).padStart(64,'0')),dispatchStatus:'complete'});assert.strictEqual(p.artifactHistory.length,500,'500-step plans must retain complete artifact history');assert.strictEqual(p.consumedArtifactHashes.length,500);console.log('artifact history retains full plan audit horizon OK');
