const fs=require('fs');const assert=require('assert');const source=fs.readFileSync('src/sidebar/sidebar.js','utf8');const needle="if (activePage === 'dashboard') setTimeout(() => loadDashboardStatus({ force: true }).catch(() => {}), 100);";const count=source.split(needle).length-1;assert.strictEqual(count,1,'UPDATE_UI must schedule one dashboard refresh, not duplicate refreshes');console.log('sidebar dashboard refresh is registered once OK');
