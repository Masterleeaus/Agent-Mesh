const fs=require('fs'),vm=require('vm'),assert=require('assert');const c={};c.globalThis=c;vm.createContext(c);for(const f of ['src/repository/repository-policy.js','src/repository/impact-engine.js'])vm.runInContext(fs.readFileSync(f,'utf8'),c,{filename:f});const files={};for(let i=0;i<120;i++)files[`app/Services/F${i}.php`]='A'.repeat(20000);const changed=Array.from({length:400},(_,i)=>`app/Models/Model${i}.php`);const r=c.CodeeImpactEngine.analyze({files},changed);assert.strictEqual(typeof r.truncatedWork,'boolean');assert.strictEqual(r.truncatedWork,true,'large repository impact scan must stop at deterministic work budget');console.log('Impact analysis has a deterministic CPU work budget');
