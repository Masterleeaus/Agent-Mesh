const fs=require('fs');const vm=require('vm');const assert=require('assert');
const html=fs.readFileSync('src/sidebar/sidebar.html','utf8');const source=fs.readFileSync('src/sidebar/sidebar.js','utf8');
for(const id of ['debugging-plan-mode','generate-debugging-plan-btn','setting-debugging-plan-default','setting-auto-recovery-sweep','setting-next-nudger-feature','setting-next-nudger-default','setting-background-watchdog','setting-composer-watchdog','setting-focus-pulse','setting-targeted-reload','setting-restore-previous-tab','setting-prevent-auto-discard']) assert(new RegExp(`id=["']${id}["']`).test(html),`missing #${id}`);
assert(!/setting-content-reinjection/.test(html),'recovery convergence must not reintroduce scripting/reinjection controls');
const context={console:{log(){},warn(){},error(){}},document:{addEventListener(){},getElementById(){return null;},querySelectorAll(){return[];},body:{classList:{toggle(){},add(){},remove(){}}}},chrome:{runtime:{onMessage:{addListener(){}}}},Map,Promise,URL};vm.runInNewContext(source,context);
const p=context.normalizePreferences({debuggingPlanDefault:true,autoRecoverySweep:false,nextNudgerFeatureEnabled:false,nextNudgerDefault:true,backgroundWatchdog:false,composerWatchdog:false,focusPulse:false,targetedReload:false,restorePreviousTab:false,preventAutoDiscard:false});
assert.strictEqual(p.debuggingPlanDefault,true);assert.strictEqual(p.autoRecoverySweep,false);assert.strictEqual(p.nextNudgerFeatureEnabled,false);assert.strictEqual(p.nextNudgerDefault,true);assert.strictEqual(p.backgroundWatchdog,false);assert.strictEqual(p.composerWatchdog,false);assert.strictEqual(p.focusPulse,false);assert.strictEqual(p.targetedReload,false);assert.strictEqual(p.restorePreviousTab,false);assert.strictEqual(p.preventAutoDiscard,false);
console.log('recovery convergence settings are present without scripting reinjection OK');
