const fs=require('fs');const assert=require('assert');const source=fs.readFileSync('src/lib/service-worker.js','utf8');const m=source.match(/CURRENT_NAVIGATION_VIEWS\s*=\s*Object\.freeze\(\[([^\]]+)\]\)/);assert(m,'CURRENT_NAVIGATION_VIEWS must exist');for(const view of ['connections','mcp'])assert(new RegExp(`['\"]${view}['\"]`).test(m[1]),`live view ${view} must be included in service-worker navigation validation`);console.log('service-worker live navigation views include connections and MCP OK');
