const fs=require('fs');const vm=require('vm');const assert=require('assert');const source=fs.readFileSync('src/lib/service-worker.js','utf8');
let sends=0;let codeeState={plan_7:{stateVersion:2,stateRevision:0,protocolMode:'signature_v2',planId:'p',runId:'r',plan:[{number:1,text:'one'}],stepIndex:0,dispatchStatus:'pending_send',currentStepId:'step-01',currentStepToken:'token-already',knownVersions:[],knownArtifactHashes:[],consumedArtifactKeys:[],target:{url:'https://chatgpt.com/c/abc',conversationIdentity:'chatgpt:abc'}}};
const chrome={sidePanel:{setPanelBehavior:async()=>{}},runtime:{onMessage:{addListener(){}},sendMessage:async()=>{}},alarms:{create(){},onAlarm:{addListener(){}}},tabs:{query(_q,cb){cb([])},get:async()=>({id:7,url:'https://chatgpt.com/c/abc'}),async sendMessage(_id,msg){if(msg.action==='GET_PAGE_STATE')return{ok:true,versions:[],artifacts:[],hasSubmittedStepToken:true};if(msg.action==='SEND_PROMPT'){sends++;return{ok:true}}return{ok:true}}},storage:{local:{get:async()=>({codeeState:JSON.parse(JSON.stringify(codeeState))}),set:async({codeeState:n})=>{codeeState=JSON.parse(JSON.stringify(n))}}}};
const c={chrome,console:{log(){},warn(){},error(){}},Map,Set,Promise,Date,Math,URL,crypto:{randomUUID:()=> 'x'}};vm.runInNewContext(source,c);
(async()=>{const r=await c.handleContentReady(7,[],[],'https://chatgpt.com/c/abc');assert.strictEqual(r.ok,true);assert.strictEqual(sends,0,'CONTENT_READY recovery must not resend a step already accepted before worker acknowledgement loss');assert.strictEqual(codeeState.plan_7.dispatchStatus,'awaiting_artifact');console.log('content-ready recovery reconciles accepted token without duplicate send OK')})().catch(e=>{console.error(e);process.exit(1)});
