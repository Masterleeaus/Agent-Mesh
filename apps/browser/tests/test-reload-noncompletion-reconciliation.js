const fs=require('fs');const vm=require('vm');const assert=require('assert');
const source=fs.readFileSync('src/lib/service-worker.js','utf8');
let codeeState={plan_7:{stateVersion:2,protocolMode:'signature_v2',planId:'p',runId:'r',plan:[{number:1,text:'one'}],stepIndex:0,versions:[],knownVersions:[],consumedArtifactHashes:[],artifactHistory:[],dispatchStatus:'awaiting_artifact',currentStepId:'step-01',currentStepToken:'tok'}};
const blocked={ready:true,protocolVersion:2,planId:'p',runId:'r',stepId:'step-01',stepToken:'tok',stepCompleted:1,stepTotal:1,status:'blocked',artifactId:'a',zip:'x.zip',type:'cumulative',version:'1',parentSha256:'N/A',sha256:'f'.repeat(64),tests:'0/1 PASS',verification:'FAIL',nextAction:'needs_user'};
const chrome={sidePanel:{setPanelBehavior:async()=>{}},runtime:{onMessage:{addListener(){}},sendMessage:async()=>({ok:true})},alarms:{create(){},onAlarm:{addListener(){}}},tabs:{query(_q,cb){cb([]);},sendMessage:async()=>({ok:true})},storage:{sync:{get:async()=>({})},local:{get:async()=>({codeeState:JSON.parse(JSON.stringify(codeeState))}),set:async({codeeState:next})=>{codeeState=JSON.parse(JSON.stringify(next));}}}};
const context={chrome,console:{log(){},warn(){},error(){}},setTimeout(fn){fn();return 1;},Map,Set,Promise,Date,Math};vm.runInNewContext(source,context);
(async()=>{const result=await context.handleContentReady(7,[],[blocked]);assert.strictEqual(result.reconciled,true,'reload handshake must reconcile a canonical blocked/failed artifact, not only completed artifacts');assert.strictEqual(codeeState.plan_7.dispatchStatus,'blocked');console.log('reload reconciles non-completed canonical status OK');})().catch(e=>{console.error(e);process.exit(1)});
