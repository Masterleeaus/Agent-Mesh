const fs=require('fs');const vm=require('vm');const assert=require('assert');
const source=fs.readFileSync('src/content-script.js','utf8');let queries=0;const composer={disabled:false,contentEditable:'true',getAttribute(){return null}};
const document={querySelector(sel){if(sel==='#prompt-textarea'||sel.includes('prompt-textarea')||sel.includes('contenteditable')){queries++;return queries>=4?composer:null;}return null},querySelectorAll(){return[]},contains(){return true},body:{textContent:''},documentElement:{textContent:''}};
const chrome={runtime:{id:'x',onMessage:{addListener(){}},sendMessage:async()=>({ok:true})}};const c={chrome,location:{href:'https://chatgpt.com/c/a',hostname:'chatgpt.com'},window:{location:{href:'https://chatgpt.com/c/a',hostname:'chatgpt.com'}},document,console:{log(){},warn(){},error(){}},setInterval(){return 1},clearInterval(){},setTimeout(fn){fn();return 1},clearTimeout(){},Event:function(){},KeyboardEvent:function(){},Set,Map,WeakSet,Array,String,Number,RegExp,JSON,URL,Date,Math,Promise,crypto:{randomUUID:()=> 'x'}};c.globalThis=c;vm.createContext(c);vm.runInContext(source,c);
(async()=>{assert.strictEqual(typeof c.waitForComposer,'function','content runtime must expose hydration-aware composer wait');const found=await c.waitForComposer({timeoutMs:1500,intervalMs:100});assert.strictEqual(found,composer,'composer appearing after SPA hydration must be detected before recovery declares failure');console.log('content composer hydration wait OK')})().catch(e=>{console.error(e);process.exit(1)});
