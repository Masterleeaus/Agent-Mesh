const fs=require('fs'),vm=require('vm'),assert=require('assert');const c={};c.globalThis=c;vm.createContext(c);for(const f of ['src/repository/repository-policy.js','src/repository/change-set.js'])vm.runInContext(fs.readFileSync(f,'utf8'),c,{filename:f});
const secret='token=super-secret';const r=c.CodeeChangeSet.create({id:secret+'X'.repeat(1000),planId:secret,stepId:'S'.repeat(1000),createdAt:'not-a-date'+secret,changes:[{path:'app/Test.php',operation:'modify'}]});const j=JSON.stringify(r);assert(!j.includes('super-secret'),'change-set metadata must be redacted');assert(String(r.id).length<=240);assert(String(r.planId).length<=240);assert(String(r.stepId).length<=240);assert(!String(r.createdAt).includes('super-secret'));console.log('Change-set metadata is bounded and redacted');
