const fs=require('fs');const vm=require('vm');const assert=require('assert');
const source=fs.readFileSync('src/lib/service-worker.js','utf8');
let codeeState={plan_7:{stateVersion:2,protocolMode:'signature_v2',planId:'p-stop',runId:'r',plan:[{number:1,text:'one'}],stepIndex:0,knownVersions:[],versions:[],consumedArtifactHashes:[],artifactHistory:[],dispatchStatus:'pending_send',target:{provider:'ChatGPT',url:'https://chatgpt.com/c/right',conversationIdentity:'chatgpt:right'}}};
let rejectSend;const sendGate=new Promise((_,reject)=>rejectSend=reject);
const chrome={sidePanel:{setPanelBehavior:async()=>{}},runtime:{onMessage:{addListener(){}},sendMessage:async()=>({ok:true})},alarms:{create(){},onAlarm:{addListener(){}}},tabs:{query(_q,cb){cb([]);},get:async()=>({id:7,url:'https://chatgpt.com/c/right'}),async sendMessage(_id,msg){if(msg.action==='GET_PAGE_STATE')return{ok:true,versions:[],artifacts:[]};if(msg.action==='SEND_PROMPT')return sendGate;return{ok:true};}},storage:{sync:{get:async()=>({})},local:{get:async()=>({codeeState:JSON.parse(JSON.stringify(codeeState))}),set:async({codeeState:next})=>{codeeState=JSON.parse(JSON.stringify(next));}}}};
const context={chrome,console:{log(){},warn(){},error(){}},setTimeout(fn){fn();return 1;},Map,Set,Promise,Date,Math,URL,crypto:{randomUUID:()=> 'x'}};vm.runInNewContext(source,context);
(async()=>{const dispatch=context.retryPendingPlanOnTab(7);await new Promise(r=>setImmediate(r));await context.stopPlan(7,'p-stop');rejectSend(new Error('tab closed'));const result=await dispatch;assert.strictEqual(result.cancelled,true,'failed in-flight send after Stop should return cancelled');assert.strictEqual(codeeState.plan_7,undefined,'dispatch catch must not resurrect a stopped plan');console.log('stop survives failed in-flight send OK');})().catch(e=>{console.error(e);process.exit(1)});
