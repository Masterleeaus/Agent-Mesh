const fs=require('fs'),vm=require('vm'),assert=require('assert');const source=fs.readFileSync('src/content-script.js','utf8');const c={console:{log(){},warn(){},error(){}},chrome:{runtime:{id:'x',onMessage:{addListener(){}}}},document:{querySelectorAll(){return[]},body:null,documentElement:null,addEventListener(){},querySelector(){return null}},window:{location:{hostname:'chatgpt.com'}},Set,Map,Promise,Date,Math,JSON,setTimeout,clearTimeout,setInterval(){return 1},clearInterval(){},MutationObserver:function(){this.observe=()=>{};this.disconnect=()=>{}},sessionStorage:{getItem(){return null},setItem(){}}};vm.createContext(c);vm.runInContext(source,c);
const base={protocolVersion:2,planId:'p',runId:'r',stepId:'s',stepToken:'t',stepCompleted:1,stepTotal:2,status:'completed',parentSha256:'',verification:'PASS',nextAction:'advance',sha256:'a'.repeat(64),zip:'x.zip'};
const a={...base,artifactId:'a:b'};const b={...base,artifactId:'a',parentSha256:'b'};
assert.notStrictEqual(c.getArtifactDetectionKey(a),c.getArtifactDetectionKey(b),'artifact detection keys must be structurally unambiguous even when fields contain separators');
console.log('Artifact detection keys are collision-safe');
