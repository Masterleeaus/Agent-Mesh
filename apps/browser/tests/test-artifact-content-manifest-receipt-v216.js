const fs=require('fs'),vm=require('vm'),assert=require('assert');const c={};c.globalThis=c;vm.createContext(c);vm.runInContext(fs.readFileSync('src/integration/artifact-verification-adapter.js','utf8'),c);const artifact={zip:'build.zip',sha256:'a'.repeat(64),zipSize:'123'};const base={verified:true,receiptId:'r',exists:true,downloadComplete:true,path:'/x/build.zip',zip:'build.zip',actualSha256:'a'.repeat(64),actualSize:123,zipIntegrity:'PASS',verifiedAt:'2026-08-16T00:00:00Z'};let r=c.CodeeArtifactVerificationAdapter.verifyReceipt(base,artifact,{requireContentManifest:true});assert.strictEqual(r.ok,false);assert.strictEqual(r.reason,'artifact-content-manifest');r=c.CodeeArtifactVerificationAdapter.verifyReceipt({...base,contentManifestVerified:true,entryCount:4},artifact,{requireContentManifest:true});assert.strictEqual(r.ok,false);assert.strictEqual(r.reason,'artifact-content-manifest');r=c.CodeeArtifactVerificationAdapter.verifyReceipt({...base,contentManifestVerified:true,entryCount:4,manifestSha256:'b'.repeat(64)},artifact,{requireContentManifest:true});assert.strictEqual(r.ok,true);assert.strictEqual(r.receipt.contentManifestVerified,true);assert.strictEqual(r.receipt.entryCount,4);console.log('strict artifact receipt requires verified ZIP content manifest OK');
