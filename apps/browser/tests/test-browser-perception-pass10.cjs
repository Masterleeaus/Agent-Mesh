const fs=require('fs'),vm=require('vm'),assert=require('assert');
const ctx={console,Date,setTimeout,clearTimeout};ctx.globalThis=ctx;ctx.chrome={debugger:{attach:async()=>{},sendCommand:async(_t,m)=>{assert.equal(m,'Runtime.evaluate');return {result:{value:{title:'Example',url:'https://example.test/',text:'Hello world',elements:[{ref:'r1',tag:'button',role:'',name:'Save',href:''}]}}};}}};vm.createContext(ctx);vm.runInContext(fs.readFileSync('src/browser/browser-perception.js','utf8'),ctx);(async()=>{const s=await ctx.CodeeBrowserPerception.snapshot(4);assert.equal(s.revision,1);assert.equal(s.elements[0].ref,'r1');const f=await ctx.CodeeBrowserPerception.find(4,'save');assert.equal(f.matches.length,1);const t=await ctx.CodeeBrowserPerception.text(4,'r1');assert.equal(t.text,'Save');const md=await ctx.CodeeBrowserPerception.pageMarkdown(4);assert(md.markdown.includes('Hello world'));assert.equal(ctx.CodeeBrowserPerception.status().titanZeroRuntimeDependency,false);console.log('Pass10 browser perception OK');})().catch(e=>{console.error(e);process.exit(1)});
