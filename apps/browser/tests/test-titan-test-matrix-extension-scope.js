const fs=require('fs'),vm=require('vm'),assert=require('assert');const c={};c.globalThis=c;vm.createContext(c);vm.runInContext(fs.readFileSync('src/titan-zero/titan-zero-test-matrix.js','utf8'),c);
const r=c.CodeeTitanZeroTestMatrix.build({domains:[]},{files:{'app/Extensions/Foo/Test.php':'<?php'}});const syntax=r.commands.find(x=>x.id==='php-syntax');assert(syntax);assert(!/not\s+-path\s+['"]app\/Extensions/i.test(syntax.command),'PHP syntax recommendation must not exclude extensions');assert(/app/.test(syntax.command));console.log('Titan verification recommendations include extension PHP');
