const fs=require('fs'),assert=require('assert');const m=JSON.parse(fs.readFileSync('manifest.json','utf8'));assert(String(m.name||'').length<=45,'manifest name must stay within Chrome metadata bounds');assert(String(m.description||'').length<=132,'manifest description must stay within Chrome metadata bounds');assert(/^\d+\.\d+\.\d+(?:\.\d+)?$/.test(String(m.version||'')),'manifest version must use Chrome numeric dotted format');console.log('manifest metadata bounds OK');
