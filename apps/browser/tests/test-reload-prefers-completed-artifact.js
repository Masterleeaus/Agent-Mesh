const fs=require('fs');const vm=require('vm');const assert=require('assert');
const source=fs.readFileSync('src/lib/service-worker.js','utf8');
const hash='a'.repeat(64);let codeeState={plan_7:{stateVersion:2,stateRevision:0,protocolMode:'signature_v2',planId:'p',runId:'r',plan:[{number:1,text:'one'}],stepIndex:0,dispatchStatus:'awaiting_artifact',currentStepId:'step-01',currentStepToken:'token-1',knownVersions:[],knownArtifactHashes:[],consumedArtifactKeys:[],consumedArtifactHashes:[],artifactHistory:[],target:{}}};
const identity={ready:true,protocolVersion:2,planId:'p',runId:'r',stepId:'step-01',stepToken:'token-1',stepCompleted:1,stepTotal:1,artifactId:'a',zip:'a.zip',sha256:hash,parentSha256:'N/A',verification:'PASS'};
const blocked={...identity,status:'blocked',nextAction:'needs_user'};const completed={...identity,artifactId:'b',status:'completed',nextAction:'advance'};
const chrome={sidePanel:{setPanelBehavior:async()=>{}},runtime:{onMessage:{addListener(){}},sendMessage:async()=>{}},alarms:{create(){},onAlarm:{addListener(){}}},tabs:{query(_q,cb){cb([])}},storage:{local:{get:async()=>({codeeState:JSON.parse(JSON.stringify(codeeState))}),set:async({codeeState:next})=>{codeeState=JSON.parse(JSON.stringify(next))}}}};
const context={chrome,console:{log(){},warn(){},error(){}},Map,Set,Promise,Date,Math,URL};vm.runInNewContext(source,context);
(async()=>{const result=await context.handleContentReady(7,[],[blocked,completed],'');assert.strictEqual(result.ok,true);assert.strictEqual(codeeState.plan_7.dispatchStatus,'complete','reload reconciliation must prefer a later valid completed footer over an older blocked footer for the same step');console.log('reload reconciliation prefers completed artifact OK')})().catch(e=>{console.error(e);process.exit(1)});
