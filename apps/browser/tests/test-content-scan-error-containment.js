const fs=require('fs');const vm=require('vm');const assert=require('assert');const source=fs.readFileSync('src/content-script.js','utf8');
let shouldThrow=false;const c={console:{log(){},warn(){},error(){}},document:{addEventListener(){},querySelectorAll(){if(shouldThrow)throw new Error('DOM churn');return[]},querySelector(){return null},documentElement:{}},chrome:{runtime:{id:'x',onMessage:{addListener(){}},sendMessage:async()=>({ok:true})}},window:{location:{hostname:'chatgpt.com'}},MutationObserver:class{observe(){} disconnect(){}},setInterval(){return 1},clearInterval(){},setTimeout(){return 1},clearTimeout(){},Set,Map,Array,String,Number,RegExp,JSON};vm.runInNewContext(source,c);shouldThrow=true;
(async()=>{let rejected=false;try{await c.checkForZIP()}catch(_e){rejected=true}assert.strictEqual(rejected,false,'polling DOM errors must be contained rather than becoming unhandled rejections');console.log('artifact scan errors are contained OK')})().catch(e=>{console.error(e);process.exit(1)});
