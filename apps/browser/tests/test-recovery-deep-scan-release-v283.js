const fs=require('fs');const assert=require('assert');const manifest=JSON.parse(fs.readFileSync('manifest.json','utf8'));const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));const readme=fs.readFileSync('README.md','utf8');const runtime=fs.readFileSync('src/integration/titan-mcp-runtime.js','utf8');const parts=manifest.version.split('.').map(Number);assert(parts[0]>2||(parts[0]===2&&(parts[1]>8||(parts[1]===8&&parts[2]>=3))));assert.strictEqual(pkg.version,manifest.version);assert(readme.includes('## v2.8.3 Deep Scan Recovery Remediation'),'README must retain deep scan remediation history');assert(runtime.includes(`clientInfo:{name:'Codee Chrome Extension',version:'${manifest.version}'}`),'MCP clientInfo must match current extension version');assert(Buffer.byteLength(manifest.description,'utf8')<=132);console.log('v2.8.3 deep scan remediation history remains compatible');
