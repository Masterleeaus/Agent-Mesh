const fs=require('fs'),vm=require('vm'),assert=require('assert');const c={};c.globalThis=c;vm.createContext(c);for(const f of ['src/repository/repository-policy.js','src/repository/mutation-envelope.js'])vm.runInContext(fs.readFileSync(f,'utf8'),c,{filename:f});const result=c.CodeeMutationEnvelope.prepare({kind:'file_write',targets:['app/Test.php'],runId:'r',payload:{content:'SECRET'.repeat(10000)},metadata:{token:'topsecret'},content:'raw-secret'});const json=JSON.stringify(result);assert(!json.includes('topsecret'));assert(!json.includes('raw-secret'));assert(!json.includes('SECRET'));assert.strictEqual(result.request.kind,'file_write');assert.deepStrictEqual(JSON.parse(JSON.stringify(result.request.targets)),['app/Test.php']);console.log('Mutation preflight exposes only bounded governance metadata');
