const fs=require('fs');const assert=require('assert');const manifest=JSON.parse(fs.readFileSync('manifest.json','utf8'));const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));const readme=fs.readFileSync('README.md','utf8');const parts=manifest.version.split('.').map(Number);assert.strictEqual(pkg.version,manifest.version,'package and manifest versions must match');assert(parts[0]>2||(parts[0]===2&&(parts[1]>8||(parts[1]===8&&parts[2]>=2))),'current release must remain at or above the v2.8.2 hotfix floor');assert(readme.includes('## v2.8.2 Background Window Composer Recovery Hotfix'),'README must retain v2.8.2 hotfix history');console.log('v2.8.2 background-window recovery history remains compatible with current release metadata');
