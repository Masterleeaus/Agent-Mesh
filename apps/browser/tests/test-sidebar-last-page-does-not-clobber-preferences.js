const fs=require('fs'),vm=require('vm'),assert=require('assert');const source=fs.readFileSync('src/sidebar/sidebar.js','utf8');const writes=[];const controls={};const document={addEventListener(){},getElementById(){return null},querySelectorAll(){return[]},body:{classList:{toggle(){},add(){},remove(){}}}};const chrome={runtime:{onMessage:{addListener(){}}},storage:{local:{get:async()=>({codeePreferences:{rememberLastPage:true,lastPage:'runner',titanZero:{enabled:false},repository:{enabled:false}},codeeLastPage:'runner'}),set:async p=>writes.push(p)}}};const c={console:{log(){},warn(){},error(){}},document,chrome,Map,Set,Promise,URL,Array,String,Number,Boolean,JSON};c.globalThis=c;vm.createContext(c);vm.runInContext(source,c);
(async()=>{await c.loadPreferences();c.rememberActivePage('diagnostics');await new Promise(r=>setTimeout(r,0));assert(writes.length>0);const last=writes[writes.length-1];assert.strictEqual(Object.prototype.hasOwnProperty.call(last,'codeePreferences'),false,'navigation persistence must not rewrite the entire preferences object');assert.strictEqual(last.codeeLastPage,'diagnostics');console.log('Sidebar last-page persistence cannot clobber capability settings');})().catch(e=>{console.error(e);process.exit(1)});
