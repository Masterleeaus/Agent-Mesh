const fs=require('fs'),assert=require('assert');const src=fs.readFileSync('src/content-script.js','utf8');assert(!src.includes('codeeSubmittedStepTokensV2'),'submitted step-token authority must not live in provider page storage');assert(!/sessionStorage[^\n]{0,160}submitted/i.test(src),'submitted control tokens must not use page sessionStorage');console.log('content control tokens are extension-owned OK');
