const fs=require('fs');const vm=require('vm');const assert=require('assert');const source=fs.readFileSync('src/content-script.js','utf8');const c={console:{log(){},warn(){},error(){}},document:{addEventListener(){},querySelectorAll(){return[]},querySelector(){return null},documentElement:{}},chrome:{runtime:{id:'x',onMessage:{addListener(){}},sendMessage:async()=>({ok:true})}},window:{location:{hostname:'chatgpt.com'}},MutationObserver:class{observe(){} disconnect(){}},setInterval(){return 1},clearInterval(){},setTimeout(){return 1},clearTimeout(){},Set,Map,Array,String,Number,RegExp,JSON};vm.runInNewContext(source,c);
assert.strictEqual(typeof c.rememberBounded,'function','content script must bound long-lived dedupe caches');const set=new Set();for(let i=0;i<1200;i++)c.rememberBounded(set,`k${i}`,1000);assert.strictEqual(set.size,1000);assert(!set.has('k0')&&set.has('k1199'));console.log('long-lived content detection caches are bounded OK');
