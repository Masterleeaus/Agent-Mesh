(function(g){'use strict';
const SCHEMA='titan-zero.manager.integration-plan.v2';
function freeze(v){if(!v||typeof v!=='object'||Object.isFrozen(v))return v;Object.freeze(v);for(const k of Object.keys(v))freeze(v[k]);return v;}
function build(input={}){if(!input.intake)throw new Error('intake required');const engine=g.TitanZeroManagerDeltaConvergence;if(!engine)throw new Error('integration analysis unavailable');const decision=engine.decide(input);const ready=decision.decision!=='BLOCKED'&&decision.decision!=='REIMPLEMENT_REQUIRED'&&input.regressionPassed===true;return freeze({schema:SCHEMA,packetId:input.intake.packetId,artifactSha256:input.intake.deltaSha256,decision:decision.decision,reason:decision.reason||null,regressionPassed:input.regressionPassed===true,readyForPullRequestGate:ready,readyForPromotionGate:ready,requiredAction:decision.decision==='CLEAN_FORWARD_PORT'?'APPLY_TRUE_DELTA':decision.decision==='SEMANTIC_REBASE'?'SEMANTIC_REBASE':decision.decision==='REIMPLEMENT_REQUIRED'?'REIMPLEMENT_REQUIRED':'BLOCKED',authority:{merge:'github-pr-merge',artifactHash:'verification-only'}});}
g.TitanZeroManagerConvergencePlan=freeze({SCHEMA,build});
})(typeof globalThis!=='undefined'?globalThis:this);
