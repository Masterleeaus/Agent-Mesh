(function(g){'use strict';
const SCHEMA='titan-zero.manager.live-state.v4';
function freeze(v){if(!v||typeof v!=='object'||Object.isFrozen(v))return v;Object.freeze(v);for(const k of Object.keys(v))freeze(v[k]);return v;}
function reconcile(input={}){const facts=input.github||input.agentMesh||null;if(!facts)return freeze({schema:SCHEMA,source:'github',status:'GITHUB_STATE_REQUIRED',drift:['github-state-required'],failClosed:true,lifecycle:null,authority:{durableTruth:'github',localProjectionOnly:true,mayClaim:false,mayUnlockDependency:false,mayComplete:false,mayRequestMerge:false}});const G=g.TitanCodeManagerGitHubState;if(!G||typeof G.derive!=='function')return freeze({schema:SCHEMA,source:'github',status:'STATE_DRIFT_DETECTED',drift:['github-state-projector-unavailable'],failClosed:true,lifecycle:null,authority:{durableTruth:'github',localProjectionOnly:true}});try{const projected=G.derive({issue:facts.issue,mainSha:facts.git?.mainSha||facts.mainSha,baseSha:facts.claim?.baseSha||facts.git?.baseSha||facts.baseSha,headSha:facts.git?.headSha||facts.claim?.headSha||facts.headSha,branch:facts.claim?.branch||facts.branch,claimBranchExists:facts.claim?.exists===true||facts.claimBranchExists===true,pr:facts.pullRequest||facts.pr,checks:Array.isArray(facts.checks)?facts.checks:(facts.checks?.items||[]),compare:facts.compare,rebaseRequired:facts.lifecycle==='REBASE_REQUIRED'||facts.rebaseRequired,blocked:facts.lifecycle==='BLOCKED',failed:facts.lifecycle==='FAILED',superseded:facts.lifecycle==='SUPERSEDED'});const drift=[];const expected=projected.git.expectedClaimBranch,pr=facts.pullRequest||facts.pr||null;if((facts.claim?.exists===true||facts.claimBranchExists===true)&&!projected.git.claimExists)drift.push('claim-branch-authority-mismatch');if(facts.claim?.branch&&expected&&facts.claim.branch!==expected)drift.push('claim-branch-noncanonical');if(pr?.headBranch&&projected.git.branch&&pr.headBranch!==projected.git.branch)drift.push('pr-head-branch-mismatch');if(pr?.baseBranch&&pr.baseBranch!=='main')drift.push('pr-base-not-main');if(pr?.headSha&&projected.git.headSha&&String(pr.headSha).toLowerCase()!==projected.git.headSha)drift.push('pr-head-sha-moved');if(String(facts.issue?.state||'').toUpperCase()==='CLOSED'&&!projected.pr?.merged)drift.push('issue-closed-without-merged-pr');if(facts.lifecycle&&facts.lifecycle!==projected.state)drift.push('projected-lifecycle-drift');return freeze({schema:SCHEMA,source:'github',status:drift.length?'STATE_DRIFT_DETECTED':'CONSISTENT',drift:[...new Set(drift)],failClosed:drift.length>0,lifecycle:projected.state,github:projected,authority:{durableTruth:'github',claim:'git-branch-ref',merge:'github-pr-merge',localProjectionOnly:true,aiMaySet:false}});}catch(error){return freeze({schema:SCHEMA,source:'github',status:'STATE_DRIFT_DETECTED',drift:['invalid-github-state-evidence'],error:String(error?.message||error).slice(0,300),failClosed:true,lifecycle:null,authority:{durableTruth:'github',localProjectionOnly:true}});}}
g.TitanZeroManagerLiveState=freeze({SCHEMA,reconcile});
})(typeof globalThis!=='undefined'?globalThis:this);