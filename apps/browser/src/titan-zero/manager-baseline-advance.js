(function(g){'use strict';
const SCHEMA='titan-zero.manager.git-baseline-advance.v2';
function freeze(v){if(!v||typeof v!=='object'||Object.isFrozen(v))return v;Object.freeze(v);for(const k of Object.keys(v))freeze(v[k]);return v;}
function clean(v,max=512){return String(v||'').trim().slice(0,max);}
function gitSha(v,label){const s=clean(v,64).toLowerCase();if(!/^[a-f0-9]{40,64}$/.test(s))throw new Error('valid '+label+' git SHA required');return s;}
function artifactSha(v){const s=clean(v,64).toLowerCase();if(s&&!/^[a-f0-9]{64}$/.test(s))throw new Error('valid artifact sha256 required');return s||null;}
function create(input={}){const mainSha=gitSha(input.mainSha||input.currentMainSha,'main'),baseSha=gitSha(input.baseSha||input.baseMainSha,'base'),headSha=gitSha(input.headSha||input.candidateHeadSha,'head');const artifactSha256=artifactSha(input.artifactSha256||input.candidateSha256);const gate=input.mergeGate||input.promotionGate||{};const allowed=!!(baseSha===mainSha&&gate.allowed===true&&input.supervisorVerdict==='PASS'&&input.regressionPassed===true&&input.reconstructionVerified===true&&['CLEAN_FORWARD_PORT','SEMANTIC_REBASE'].includes(input.convergenceDecision));return freeze({schema:SCHEMA,mainSha,baseSha,headSha,artifactSha256,artifact:clean(input.artifact,512),convergenceDecision:clean(input.convergenceDecision,64),supervisorVerdict:clean(input.supervisorVerdict,64),regressionPassed:input.regressionPassed===true,reconstructionVerified:input.reconstructionVerified===true,status:allowed?'READY_FOR_GITHUB_MERGE':'BLOCKED',canonical:false,githubMergeRequired:true,authority:{baseline:'git-main-sha',merge:'github-pr-merge',artifactHash:'verification-only'}});}
g.TitanZeroManagerBaselineAdvance=freeze({SCHEMA,create});
})(typeof globalThis!=='undefined'?globalThis:this);
