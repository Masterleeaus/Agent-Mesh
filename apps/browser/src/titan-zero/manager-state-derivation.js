(function(g){'use strict';
const SCHEMA='titan-zero.manager.derived-state.v4';
function freeze(v){if(!v||typeof v!=='object'||Object.isFrozen(v))return v;Object.freeze(v);for(const k of Object.keys(v))freeze(v[k]);return v;}
function arr(v){return Array.isArray(v)?v:[];}
function derive(input={}){const live=input.liveReconciliation||input.live||null,github=input.githubProjection||live?.github||null;if(!github)return freeze({schema:SCHEMA,source:'github',status:'GITHUB_STATE_REQUIRED',failClosed:true,drift:['github-state-required'],lifecycle:null,queue:null,restart:{reconstructed:false,integrityVerified:false,reconcileWithGitHubRequired:true},authority:{durableTruth:'github',localProjectionOnly:true,mayClaim:false,mayUnlockDependency:false,mayComplete:false,mayRequestMerge:false}});const drift=arr(live?.drift).map(code=>({code}));const workItems=arr(input.workItems||input.githubItems);const queue=g.TitanZeroManagerQueueState?g.TitanZeroManagerQueueState.projectGithub({workItems}):null;return freeze({schema:SCHEMA,source:'github',status:live?.status||(drift.length?'STATE_DRIFT_DETECTED':'CONSISTENT'),failClosed:live?.failClosed===true,drift,lifecycle:github.state,github,queue,restart:{reconstructed:false,integrityVerified:false,reconcileWithGitHubRequired:true},authority:{durableTruth:'github',claim:'git-branch-ref',merge:'github-pr-merge',localProjectionOnly:true}});}
g.TitanZeroManagerStateDerivation=freeze({SCHEMA,derive});
})(typeof globalThis!=='undefined'?globalThis:this);