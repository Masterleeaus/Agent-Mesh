const clean=v=>String(v??'').trim();
export function buildSchedulingRecommendation(input={}){const company_id=clean(input.company_id);if(!company_id)throw new Error('company_id-required');const candidates=(input.candidates||[]).filter(x=>clean(x.company_id)===company_id).map(x=>({...x,worker_id:clean(x.worker_id),score:Number(x.score||0)})).filter(x=>x.worker_id).sort((a,b)=>b.score-a.score||a.worker_id.localeCompare(b.worker_id));return {schema:'titan.workforce.starter.scheduling-recommendation.v1',company_id,work_item_id:clean(input.work_item_id)||null,candidates,recommended_worker_id:candidates[0]?.worker_id||null,state:candidates.length?'PROPOSED':'NO_MATCH',requires_human_approval:true,execution_permitted:false,grants_authority:false};}
export function buildSchedulingHandoff(rec={}){if(!clean(rec.company_id))throw new Error('company_id-required');return {schema:'titan.workforce.starter.scheduling-handoff.v1',company_id:rec.company_id,work_item_id:rec.work_item_id||null,recommended_worker_id:rec.recommended_worker_id||null,dedupe_key:`${rec.company_id}:schedule:${rec.work_item_id||'unknown'}`,direct_mutation:false,execution_permitted:false,grants_authority:false};}
