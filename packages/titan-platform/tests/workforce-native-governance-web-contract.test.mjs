import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";import {fileURLToPath} from "node:url";const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"../../..");
test("all six web adapters attach shared governance evidence",()=>{for(const name of ["reception","sales","booking","scheduling","jobs","customer-care"]){const text=fs.readFileSync(path.join(root,`apps/web/lib/titan/workforce-native/${name}.ts`),"utf8");assert.match(text,/buildNativeGovernance/);assert.match(text,/governance/);}});
test("shared governance threads trace, audit and offline mode headers",()=>{const text=fs.readFileSync(path.join(root,"apps/web/lib/titan/workforce-native/governance.ts"),"utf8");assert.match(text,/x-titan-audit-trace-id/);assert.match(text,/x-titan-governance-offline-mode/);assert.match(text,/native-route/);});
