{
  "schema": "titan-zero-offline-pass05-semantic-change/v1",
  "packet_id": "TZ-FINISH-OFFLINE-RESILIENCE-001",
  "pass": 5,
  "semantic_delta": "Adds durable restart checkpoints, restart recovery-required transitions, explicit idempotency-bound resume, exactly-once continuation claims, durable lifecycle evidence and monotonic restart event ordering. Restarts never automatically replay effects."
}
