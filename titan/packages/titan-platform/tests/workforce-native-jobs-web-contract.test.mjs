import test from "node:test";import assert from "node:assert/strict";import fs from"node:fs";import path from"node:path";const root=path.resolve(new URL("../../../",import.meta.url).pathname);const adapter=fs.readFileSync(path.join(root,"apps/web/lib/titan/workforce-native/jobs.ts"),"utf8");const route=fs.readFileSync(path.join(root,"apps/web/app/api/v1/titan/workforce/native/jobs/route.ts"),"utf8");test("Jobs adapter delegates only canonical plans",()=>{assert.match(adapter,/fetch\(target/);assert.match(adapter,/session\.accountId/);assert.match(adapter,/x-titan-workforce-agent/);assert.doesNotMatch(adapter+route,/chrome\.runtime|browser\.runtime/);});test("Jobs endpoint is owner admin guarded",()=>{assert.match(route,/withRole\(\["owner","admin"\]/);for(const action of["evaluate_transition","transition_project","start_visit","transition_visit","evaluate_evidence","evaluate_completion","complete_work_order","classify_exception","build_offline_mutation","detect_revision_conflict"])assert.match(route,new RegExp(action));});
