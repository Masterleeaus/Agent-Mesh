import{describe,expect,it}from"vitest";import{buildBusinessRuntimeContext,buildWorkforceContextCard}from"../business-context";
const attention={id:"a1",account_id:"c1",type:"invoice.paid",entity_type:"invoice",entity_id:"i1",title:"Invoice paid",summary:null,href:"/i/1",dedupe_key:null,created_at:"2026-01-01",read_at:null};
describe("Interface Runtime business context",()=>{it("binds zero attention decisions jobs and workforce without authority",()=>{const c=buildBusinessRuntimeContext({company_id:"c1",surface:"zero",attention:[attention],decisions:[{id:"d1",title:"Approve quote"}],activeJobs:[{id:"j1",title:"Kitchen clean",status:"active"}]});expect(c.attention).toHaveLength(1);expect(c.decisions).toHaveLength(1);expect(c.working_set[0].object_id).toBe("j1");expect(c.workforce?.counts.agents).toBeGreaterThan(0);expect(c.business_writes).toBe(false);expect(buildWorkforceContextCard(c)?.executionAuthority).toBe(false)});it("rejects cross-company attention",()=>expect(buildBusinessRuntimeContext({company_id:"c2",surface:"zero",attention:[attention]}).attention).toHaveLength(0));it("keeps Hub customer-safe",()=>{const c=buildBusinessRuntimeContext({company_id:"c1",surface:"hub",attention:[attention],decisions:[{id:"d1"}],services:[{id:"s1",title:"Weekly clean",status:"booked"}]});expect(c.attention).toHaveLength(0);expect(c.decisions).toHaveLength(0);expect(c.working_set[0].object_key).toBe("service");expect(c.workforce).toBeNull()});it("requires company_id",()=>expect(()=>buildBusinessRuntimeContext({company_id:"",surface:"go"})).toThrow("company_id-required"))});
