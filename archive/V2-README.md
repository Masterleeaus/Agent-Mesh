# Agent Mesh V2 Archive Notice

The former custom V2 version-control/control-plane machinery is retired from active development authority.

Historical V2 commits, custom HEAD, branch pointers, leases, reflogs, CAS/promotion transactions, persistence guards and ZIP checkpoints may be retained as historical/recovery evidence, but must not be used to determine current code authority.

GitHub main + Git commit SHA now provide code authority and history.
