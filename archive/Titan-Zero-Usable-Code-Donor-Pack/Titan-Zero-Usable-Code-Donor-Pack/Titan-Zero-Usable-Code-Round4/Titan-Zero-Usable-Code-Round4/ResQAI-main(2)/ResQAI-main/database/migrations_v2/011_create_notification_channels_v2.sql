-- ResQAI V2 Migration 011
-- Date:    2026-06-29
-- Purpose: Create notification_channels_v2 table for delivery channel config
--
-- Applied to: ResQAI V2 Pod
-- Schema: v2

-- ============================================================
-- Step 1: Create notification_channels_v2 table
-- ============================================================
-- lemma table create notification_channels_v2 \
--   id:UUID --pk \
--   channel_type:TEXT \
--   provider:TEXT \
--   config:JSONB \
--   is_enabled:BOOLEAN --default true \
--   rate_limit:INTEGER --default 100 \
--   last_health_check:TIMESTAMPTZ \
--   last_error:TEXT \
--   created_at:TIMESTAMPTZ \
--   updated_at:TIMESTAMPTZ

-- ============================================================
-- Step 2: Add indexes
-- ============================================================
-- lemma table add-index notification_channels_v2 idx_nchannels_type --using btree --fields channel_type
-- lemma table add-index notification_channels_v2 idx_nchannels_enabled --using btree --fields is_enabled

-- Verify: lemma table describe notification_channels_v2
-- Verify: lemma table indexes notification_channels_v2
-- Expected: idx_nchannels_type, idx_nchannels_enabled
