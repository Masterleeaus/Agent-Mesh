# FastAPI and ASGI server (compatible with MCP anyio requirement)
fastapi>=0.109.0
uvicorn[standard]>=0.25.0

# MCP protocol (using latest available version)
mcp==1.26.0

# HTTP client for external APIs
httpx>=0.27.1

# Validation and settings (updated for compatibility)
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Testing (flexible versions)
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-mock>=3.12.0
respx>=0.20.2

# Development (flexible versions)
black>=23.11.0
isort>=5.12.0
mypy>=1.7.1