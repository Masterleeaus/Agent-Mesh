{
  "type": "object",
  "title": "FlagSlippingFollowupsInput",
  "properties": {
    "today": { "anyOf": [{ "type": "string", "format": "date" }, { "type": "null" }], "title": "Today", "default": null, "description": "Override 'now'. Defaults to system date." },
    "top_n": { "type": "integer", "title": "Top N", "default": 20, "description": "Cap result size for UI lists." },
    "days_ahead": { "type": "integer", "title": "Days Ahead", "default": 7, "description": "Days from today to consider 'due soon'." },
    "include_statuses": { "type": "array", "items": { "type": "string" }, "title": "Include Statuses", "description": "Statuses that are still 'open'." }
  }
}
