{
  "type": "object",
  "title": "UpdateTicketRecordOutput",
  "required": ["status", "ticket_id"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["success", "not_found", "error"],
      "description": "Operation result status."
    },
    "ticket_id": {
      "type": "string",
      "description": "Echoed ticket_id for workflow tracking."
    },
    "error": {
      "type": "string",
      "description": "Error detail if the update failed."
    }
  }
}
