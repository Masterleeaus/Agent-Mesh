{
  "type": "object",
  "required": ["ticket_id"],
  "properties": {
    "ticket_id": {
      "type": "string",
      "description": "Ticket id to draft a reply for. Required."
    },
    "channel_hint": {
      "type": "string",
      "enum": ["email", "sms", "web", "phone", "chat"],
      "description": "Optional. Override the channel inferred from the ticket for channel-specific phrasing."
    },
    "override_technician": {
      "type": "string",
      "description": "Optional. Pin the suggested owner to this technician name (must exist in technicians table)."
    }
  }
}
