{
  "type": "object",
  "required": ["ticket_id"],
  "properties": {
    "ticket_id": {
      "type": "string",
      "description": "Ticket id to read and suggest a technician for. Required."
    },
    "force_resuggest": {
      "type": "boolean",
      "description": "If true, recompute suggestion even when the ticket is already assigned. Default false."
    }
  }
}
