{
  "type": "object",
  "required": ["dispute_id"],
  "properties": {
    "dispute_id": {
      "type": "string",
      "description": "Dispute id to analyze. Required."
    },
    "force_reanalysis": {
      "type": "boolean",
      "description": "If true, recompute even when the dispute is already in 'recommendation_ready'. Default false."
    }
  }
}
