import test from 'node:test';import assert from 'node:assert/strict';import {listStarterAgents,getStarterAgent} from '../../titan-workforce/starter-agents/starter-agent-registry.mjs';test('ten canonical starter agents are registered',()=>{const a=listStarterAgents();assert.equal(a.length,10);for(const k of ['reception','sales','quote','booking','scheduling','dispatch','jobs','invoicing','customer-care','rebooking'])assert.equal(getStarterAgent(k)?.enabled,true);assert.ok(a.every(x=>x.company_boundary==='company_id'&&x.identity_grants_authority===false));});
